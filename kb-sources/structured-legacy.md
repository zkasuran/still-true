---
title: Structured outputs (legacy)
source: Anthropic API docs, earlier structured-output guidance
period: early 2025
authority: official (historical)
---

# Getting structured output

Pass the top-level `output_format` parameter on `messages.create` to request
structured JSON output.
